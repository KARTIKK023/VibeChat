services:
  - type: web
    name: chat-app
    env: node
    plan: free
    buildCommand: |
      npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
      - key: NODE_VERSION
        value: 20
      